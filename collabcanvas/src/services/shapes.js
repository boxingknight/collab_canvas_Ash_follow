import { db } from './firebase';

// TODO: Implement shapes service functions

export async function addShape(shape) {
  // Add shape to Firestore
}

export async function updateShape(shapeId, updates) {
  // Update shape in Firestore
}

export async function deleteShape(shapeId) {
  // Delete shape from Firestore
}

export function subscribeToShapes(callback) {
  // Subscribe to shape changes in Firestore
  // Return unsubscribe function
}

